## 1. HTML — Header de sección

- [x] 1.1 Reemplazar el `<header>` de `mindustry.html` por la nueva estructura compacta: marca "SOBRE LA MARCHA", separador, nombre de sección, descripción, enlace de retorno `← Volver al inicio` y enlace `▶ Lista de vídeos`
- [x] 1.2 Mover la build note del `<header>` al inicio de `<main>`, antes del toolbar de búsqueda

## 2. CSS — Estilos del header de sección

- [x] 2.1 Crear la clase `.section-header` con estilos para el header compacto: padding reducido, marca en JetBrains Mono small, h1 de sección como foco, layout de acciones de navegación
- [x] 2.2 Aplicar la clase `section-header` al `<header>` de `mindustry.html`

## 3. Verificación

- [x] 3.1 Verificar que `index.html` no tiene cambios y su header se muestra correctamente
- [x] 3.2 Verificar que `mindustry.html` muestra el header compacto con marca, nombre de sección, back button y enlace a YouTube
- [x] 3.3 Verificar que la build note aparece en `<main>` antes del toolbar
- [x] 3.4 Verificar que el enlace "Volver al inicio" navega a `index.html`
- [x] 3.5 Verificar que el enlace "Lista de vídeos" abre la playlist en nueva pestaña
